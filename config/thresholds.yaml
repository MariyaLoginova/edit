# Пороги и веса — в конфиг, не в код (см. EDIT-00 §7).
# До ~15-20 роликов это гипотеза; калибруется G1 на реальных отвалах.

retention:
  # E2: выше = хуже. passes=false если score >= threshold ИЛИ есть severity>=4
  dropoff_score_threshold: 40

material:
  # C1/C2: размеры пачек поиска
  web_results: 5
  image_results: 8
  # FIX-2: минимум картинок на каждое состояние A/B для freeze
  min_images_per_state: 3

scenario:
  # FIX-3: короче + отложенный payoff (механизм ≥55% длительности)
  target_duration_sec: 45
  min_duration_sec: 38
  max_duration_sec: 52
  meta_stop_phrases:
    - "странно, но"
    - "формула простая"
    - "заметь"
    - "через миг"
    - "на самом деле всё просто"
    - "а теперь представь"
    - "механизм:"
    - "формула:"
    - "в материале"
    - "в сниппете"
    - "как сказано"
    - "считывается как"

editorial:
  opening_variants_min: 5
  opening_variants_max: 8
  # E6 удалён (FIX-4); пороги оставлены на случай offline-утилит
  compress_min_ratio: 0.20
  compress_max_ratio: 0.25
  dropoff_score_threshold: 40
  attack_severity_block: 4

shots:
  # F1: сколько картинок на фразу
  images_per_phrase: 5

scoring:
  # B1: 5 осей (веха 5). G1 двигает weights по метрикам.
  weights:
    surprise: 1.0
    specificity: 1.0
    causal_clarity: 1.0
    evidence: 1.0
    shareability: 1.0

# EDIT-B1: пакетное ранжирование тем на первом проходе по книге.
# Приоритет — желания аудитории (пересылка / необычный факт), не оценка автора.
# showable — мягкая ось: низкий балл не роняет тему в bank сам по себе.
# Веса — гипотеза до 15–20 роликов с фактическими метриками.
topic_scoring:
  min_axis_for_production: 2
  produce_threshold: 3.4
  batch_size: 20
  soft_axes:
    - showable
  require_author_conclusion: false
  weights:
    showable: 0.5
    surprise: 1.5
    recognizable: 1.0
    social_currency: 1.6
    arguable: 0.9
    supersystem: 0.9

learning:
  # После стольких роликов hypothesis=false (пока ориентир)
  min_rollouts_for_calibration: 15
